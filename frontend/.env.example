# ── Genessence frontend (Vite) environment variables ───────────────────────
# Copy to `.env` for local dev. In production (Render Static Site), set VITE_API_URL
# in the service's Environment. Vite inlines this at BUILD time, so a redeploy is
# required after changing it.

# Base URL of the backend. You may include the /api suffix or not — the app
# normalizes it to always end in exactly one "/api".
#   Local dev:   http://localhost:5050/api   (or http://localhost:5050)
#   Production:  https://genessenceos.onrender.com/api   (or https://genessenceos.onrender.com)
VITE_API_URL=http://localhost:5050/api
